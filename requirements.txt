# Core ML
torch==2.11.0
numpy==2.4.3
scipy==1.17.1

# AI
anthropic           # native SDK — required for software_company agent tooling
google-genai==1.68.0
# Previously required (now removed — safe to uninstall):
#   pip uninstall langchain-anthropic langchain-core langgraph -y

# Web / API
requests==2.32.5
httpx==0.28.1
ddgs>=9.0.0

# Visualization
matplotlib==3.10.8

# Backend
fastapi
uvicorn

# Utils
pydantic==2.12.5
tenacity==9.1.4
tqdm==4.67.3

python-dotenv

# Desktop automation (optional at runtime; required for window tools + pyautogui flows)
PyGetWindow
# uiautomation — optional; Windows-only desktop_uia_* tools (UI Automation list/read/click by name)

# Dev / Testing
pytest==9.0.2
