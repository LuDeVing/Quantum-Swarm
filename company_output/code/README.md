# Personal Finance Tracker

## How to run the app

1.  Set the `ENCRYPTION_KEY` environment variable. You can generate a strong key using a tool like `openssl rand -base64 32`.
2.  Run `docker-compose up`.
3.  Open `index.html` in a browser.

## Screenshot

![Personal Finance Tracker Screenshot](screenshot.png)